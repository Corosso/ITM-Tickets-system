apiVersion: v1
kind: Service
metadata:
  name: price-api
  namespace: itm-tickets
  labels:
    app: price-api
spec:
  type: ClusterIP
  selector:
    app: price-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
