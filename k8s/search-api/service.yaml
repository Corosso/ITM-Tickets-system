apiVersion: v1
kind: Service
metadata:
  name: search-api
  namespace: itm-tickets
  labels:
    app: search-api
spec:
  type: ClusterIP
  selector:
    app: search-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
